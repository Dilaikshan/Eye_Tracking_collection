import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color primary = Color(0xFF5E0668);
  static const Color accent = Color(0xFF791085);

  // High contrast colors for targets (for partially blind users)
  static const Color targetRed = Color(0xFFFF0000);
  static const Color targetYellow = Color(0xFFFFFF00);
  static const Color targetGreen = Color(0xFF00FF00);
  static const Color targetBlue = Color(0xFF0000FF);
  static const Color targetMagenta = Color(0xFFFF00FF);
  static const Color targetCyan = Color(0xFF00FFFF);

  // Status colors
  static const Color alignedGreen = Color(0xFF00FF88);
  static const Color notAlignedRed = Color(0xFFFF4444);

  // Legacy support
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFCCCCCC);
  static const Color danger = Color(0xFFFF3B30);
}
